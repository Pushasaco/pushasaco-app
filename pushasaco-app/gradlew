#!/usr/bin/env sh
# Minimal wrapper shim. If you prefer, run with Android Studio or install a gradle wrapper locally.
./gradlew.bat "$@" 2>/dev/null || ( echo "Use Android Studio or install Gradle 8.7+ locally."; exit 0 )