# Terraform stubs

This folder contains stubs to provision supporting resources later:
- App Check enforcement toggles
- Pub/Sub topics and BigQuery datasets
- IAM least-privilege roles for Functions and Cloud Run

> Fill in `project_id` and providers before `terraform init`.